              <% unless @multiresimage.VRA.find_by_terms(:image, :relationSet, :relation).text.empty? %>
                  <li>Related Images
                    <ul>
                      <div class="gallery_container">
                        <% @multiresimage.related_ids.each do |work_pid| %>
                            <div class="listing"><a href="/catalog/<%= work_pid %>"><img src="<%= DIL_CONFIG['dil_app_url']%><%= work_pid %><%= DIL_CONFIG['dil_fedora_disseminator_thumbnail']%>"></a></div>
                        <% end %>
                      </div>
                    </ul>
                  </li>

              <% end %>

